@model IEnumerable<FlightReservationsApplication.Models.ReservationConfirmation>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ConfirmationDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DeclinedDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Notes)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Reservation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Employee)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ConfirmationDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DeclinedDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Notes)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Reservation.ReservationID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Employee.EmployeeID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ReservationConfirmationID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ReservationConfirmationID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ReservationConfirmationID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
